# E2E 測試報告 — 2026-07-05

> 執行者：Claude（Playwright + 本機 dev server `localhost:3000`）
> 版本：master @ 30a2d4a｜React 18 + CRA(react-scripts 5.0.1) + TS 4.9 + Tailwind

## 測試計畫（先規劃後執行）

| # | 區塊 | 方法 | 結果 |
|---|------|------|------|
| 0 | 靜態品質：typecheck / 單元測試 / production build | CLI | ✅ |
| A | 首次載入：Onboarding 自動彈出、4 範例遊戲 seed | 清 localStorage 後 reload | ✅ |
| B | 遊戲執行（淘金熱）：動作→供給扣減→規則觸發→事件日誌 | Playwright 操作 | ✅ |
| C | 遊戲編輯器：畫布/zone 渲染、元件選取→屬性面板 | Playwright | ✅ |
| D | 規則編輯器：觸發時機/條件類型/AND-OR/動作 | Playwright | ✅ |
| E | 遊戲執行（骰子冒險）：擲骰 d6 + 遊戲變數 | Playwright | ✅ |
| F | 模組管理：新建遊戲 Modal + 空白建立（4→5） | Playwright | ✅ |
| G | 響應式：手機 390×844 | resize | ⚠️ 首頁 OK／編輯器・執行頁 **不可用** |
| H | Console error / 引擎架構審計 | console + 讀碼 | ✅ 0 error |

## 詳細結果

### 0. 靜態品質
- `react-scripts test --watchAll=false`：**40 / 40 通過**（含供給 T1–T6、AND/OR、觸發鏈、edge cases）。
- `tsc --noEmit`：應用碼乾淨；僅測試檔報 jest 全域型別缺失（不影響 runtime，屬 `@types/jest` 未裝）。
- `react-scripts build`：**成功**，gzip 後 main.js 106 KB / css 6.47 KB。
- `src/` 內 **0 個** TODO/FIXME 殘留。

### A. 首次載入
- 清 localStorage 後 reload：Onboarding Tour 自動彈出（1/7），可「跳過」。
- 4 範例遊戲正確 seed：桌遊大師 Demo、淘金熱、卡牌大師、骰子冒險（BUG-001 同 ms 撞 ID 未再現）。
- 證據：`e2e-01-firstload.png`

### B. 淘金熱完整迴圈
- 開始遊戲 → 執行「採礦」：玩家獲得 `礦石 ×2`，供給池 `礦石 30→28`，`score_sell` 規則於 onActionEnd 觸發 +1 分，事件日誌逐筆記錄。
- 供給殘量為即時衍生（`getSupplyRemaining = supply − Σ玩家持有 − Σ牌堆`），無第二真相。
- 視覺棋盤（RuntimeBoard）正確渲染：玩家區（橘虛線框）＋供給池（綠框＋籌碼×N）。
- 證據：`e2e-02-goldrush-board.png`、`e2e-03-goldrush-play.png`

### C. 遊戲編輯器
- 5 分頁（元件/動作/玩家/變數/格子）、籌碼造型 token、畫布 zone、工具列（主題色/棋盤設定/玩家區/供給池/垃圾桶/匯出 JSON）。
- 選取 token → 右側屬性面板顯示 名稱/圖示/類型/供給總量/ID，可編輯。
- 證據：`e2e-04-editor.png`

### D. 規則編輯器
- 觸發時機 4 種（onActionEnd / onTurnEnd / onObjectChange / onTokenLand）。
- 條件類型 7 種（含 hasTokenCount、hasScore、tokenAtCellIndex、tokenOnCellType、hasVariable、AND、OR）。
- 證據：`e2e-05-ruleeditor.png`

### E. 骰子冒險
- 「探索（擲骰）」執行：事件日誌 `擲骰（d6）：結果 6`；遊戲變數「體力」顯示。
- 註：此遊戲無 zones/cells → 執行頁不畫視覺棋盤，只有文字面板。
- 證據：`e2e-06-dice.png`

### F. 模組管理
- 「新建遊戲」Modal：空白遊戲 + 4 個範本可選；建立空白遊戲後清單 4→5。
- 匯出：`downloadGameModule` 走 Blob + `URL.createObjectURL` 下載（讀碼確認）。
- 證據：`e2e-09-newgame-modal.png`

### G. 響應式（關鍵缺陷）
- **首頁**：手機 390px 單欄堆疊、按鈕自適應，OK。證據：`e2e-08-mobile-home.png`
- **遊戲執行頁**：390px 下仍維持 玩家/動作/規則 三欄硬排，文字被壓成直行、規則欄溢出右緣，**實質不可用**。證據：`e2e-07-mobile-play.png`
- **編輯器**：固定 800×500 畫布 + 側欄；且 `react-dnd-html5-backend` 不支援 touch，手機無法拖放。→ 編輯器目前是桌機專用。

### H. Console / 架構審計
- 全程 console **0 error / 0 warning**。
- `RuleEngine` 為 pure class、單一真相（供給/位置皆即時衍生）、支援 loadState 快照，架構乾淨。
- 永續層 `moduleStorage`：**100% localStorage**，無後端、無帳號、無雲端。

## 未涵蓋（留待人工或後續）
- 匯出檔案的實際下載內容比對（Playwright 未攔截下載串流）。
- 卡牌大師牌堆合成完整流程、格子軌道 moveToken 落地觸發（已由單元測試 + M5 EVIDENCES 覆蓋）。
- 觸發鏈深度上限（MAX_CHAIN_DEPTH=5）UI 警告、undo/redo 快捷鍵、快速測試 Drawer。
